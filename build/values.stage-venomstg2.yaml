env:
  RUST_LOG: info
  RUST_BACKTRACE: 1
  DATABASE_URL: postgresql://$(BACKEND_API_USER):$(BACKEND_API_PASSWORD)@cloudsqlproxy.default:5432/indexer-venom-stg-2
  INDEXER_API_DATABASE_URL: postgresql://$(BACKEND_API_USER):$(BACKEND_API_PASSWORD)@cloudsqlproxy.default:5432/indexer-venom-stg-2
  indexer_api__http_address: 0.0.0.0:8080
  indexer_api__database__max_connections: 20
  indexer_api__jwt_secret: $(VENOM_API_JWT_SECRET)
  indexer_api__auth_token_lifetime: "31536000"
  indexer_api__base_url: testnet.oasis.gallery

controller: statefulset

service:
  port: 8080

autoscaling:
  enabled: false
  minReplicas: 5

ingress:
  enabled: true
  hosts:
    - host: indexer-api-venom-stg2.bf.works
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: indexer-api-venom-stg2-tls
      hosts:
        - indexer-api-venom-stg2.bf.works

livenessProbe:
  httpGet:
    path: /healthz
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /healthz
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 10
